<?php

declare(strict_types=1);

// Code generated by surface-gen; DO NOT EDIT.

namespace MessageBird\Resources;

use MessageBird\Core\Page;
use MessageBird\RequestOptions;
use MessageBird\Wire\Model\EmailBroadcast;
use MessageBird\Wire\Model\EmailBroadcastClickedLinkList;
use MessageBird\Wire\Model\EmailBroadcastCounts;
use MessageBird\Wire\Model\EmailBroadcastList;
use MessageBird\Wire\Model\EmailBroadcastSendQuota;
use MessageBird\Wire\Model\EmailEvent;
use MessageBird\Wire\Model\EmailEventList;
use MessageBird\Wire\Model\EmailRecipient;
use MessageBird\Wire\Model\EmailRecipientList;

class BroadcastsBase extends Resource
{
    /**
     * List broadcasts in the workspace, newest first. Filter by lifecycle `status`, `audience_id`, or `tag`, search tag names/values with `q`, or narrow to a window with `created_after`/`created_before`. Use `broadcasts.get` for full detail on one broadcast.
     *
     * @param array<string, mixed>|null $query query parameters (untyped for now)
     *
     * @return Page<EmailBroadcast>
     *
     * @example Iterate every broadcast, or take one page
     * foreach ($bird->broadcasts->list(['status' => ['sent']]) as $broadcast) {
     *     echo $broadcast->getId(), ' ', $broadcast->getStatus(), PHP_EOL;
     * }
     */
    public function list(?array $query = null, ?RequestOptions $options = null): Page
    {
        return $this->paginate(
            EmailBroadcast::class,
            fn (?string $cursor): EmailBroadcastList => $this->single('GET', '/v1/email/broadcasts', EmailBroadcastList::class, null, $cursor === null ? ($query ?? []) : array_merge(array_diff_key($query ?? [], ['ending_before' => true]), ['starting_after' => $cursor]), $options),
            static function (object $page): iterable {
                \assert($page instanceof EmailBroadcastList);

                return $page->getData() ?? [];
            },
            static function (object $page): ?string {
                \assert($page instanceof EmailBroadcastList);

                return $page->getNextCursor();
            },
        );
    }

    /**
     * Fetch one broadcast by its `broadcast_id`, with its audience reference and counters. For the full recipient list use `broadcasts.list_recipients`, and for the delivery timeline use `broadcasts.list_events`.
     *
     * @example Fetch a broadcast by id
     * $broadcast = $bird->broadcasts->get('eb_01krdgeqcxet5s7t44vh8rt9mg');
     * echo $broadcast->getStatus(), ' ', $broadcast->getSentCount(), ' ', $broadcast->getDeliveredCount();
     */
    public function get(string $broadcastId, ?RequestOptions $options = null): EmailBroadcast
    {
        return $this->single('GET', '/v1/email/broadcasts/' . rawurlencode($broadcastId), EmailBroadcast::class, null, null, $options);
    }

    /**
     * Permanently delete a draft broadcast. Only a draft can be deleted. To stop a scheduled or sending broadcast, use `broadcasts.cancel` instead.
     *
     * @example Delete a draft broadcast
     * $bird->broadcasts->delete('eb_01krdgeqcxet5s7t44vh8rt9mg');
     */
    public function delete(string $broadcastId, ?RequestOptions $options = null): void
    {
        $this->none('DELETE', '/v1/email/broadcasts/' . rawurlencode($broadcastId), null, null, $options);
    }

    /**
     * Cancel a scheduled, accepted, or sending broadcast. This stops every delivery that has not gone out yet. Recipients already dispatched are not recalled. Calling this again on a broadcast already canceling or canceled returns its current state rather than an error. A draft cannot be canceled, because it was never sent. Delete it with `broadcasts.delete` instead.
     *
     * @example Stop a scheduled or sending broadcast
     * $broadcast = $bird->broadcasts->cancel('eb_01krdgeqcxet5s7t44vh8rt9mg');
     * echo $broadcast->getStatus(); // "canceling" or "canceled"
     */
    public function cancel(string $broadcastId, ?RequestOptions $options = null): EmailBroadcast
    {
        return $this->single('POST', '/v1/email/broadcasts/' . rawurlencode($broadcastId) . '/cancel', EmailBroadcast::class, null, null, $options);
    }

    /**
     * List the per-recipient delivery timeline for a broadcast (sends, opens, clicks, bounces, and so on), oldest first, as a cursor page. Filter by `type`, for example `email.bounced` or `email.opened`.
     *
     * @param array<string, mixed>|null $query query parameters (untyped for now)
     *
     * @return Page<EmailEvent>
     *
     * @example Iterate a broadcast's delivery timeline
     * $events = $bird->broadcasts->listEvents('eb_01krdgeqcxet5s7t44vh8rt9mg', ['type' => 'email.bounced']);
     * foreach ($events as $event) {
     *     echo $event->getType(), ' ', $event->getRecipientId(), ' ', $event->getBounceType(), PHP_EOL;
     * }
     */
    public function listEvents(string $broadcastId, ?array $query = null, ?RequestOptions $options = null): Page
    {
        return $this->paginate(
            EmailEvent::class,
            fn (?string $cursor): EmailEventList => $this->single('GET', '/v1/email/broadcasts/' . rawurlencode($broadcastId) . '/events', EmailEventList::class, null, $cursor === null ? ($query ?? []) : array_merge(array_diff_key($query ?? [], ['ending_before' => true]), ['starting_after' => $cursor]), $options),
            static function (object $page): iterable {
                \assert($page instanceof EmailEventList);

                return $page->getData() ?? [];
            },
            static function (object $page): ?string {
                \assert($page instanceof EmailEventList);

                return $page->getNextCursor();
            },
        );
    }

    /**
     * List the recipients resolved for a broadcast with their per-recipient delivery state, paginated. It shows who a broadcast reached and how far each one got. The list is empty until the broadcast starts sending. Pass `to` to look up one address instead of paging the whole set.
     *
     * @param array<string, mixed>|null $query query parameters (untyped for now)
     *
     * @return Page<EmailRecipient>
     *
     * @example Iterate the recipients a broadcast reached
     * foreach ($bird->broadcasts->listRecipients('eb_01krdgeqcxet5s7t44vh8rt9mg') as $recipient) {
     *     echo $recipient->getRecipient(), ' ', $recipient->getStatus(), PHP_EOL;
     * }
     */
    public function listRecipients(string $broadcastId, ?array $query = null, ?RequestOptions $options = null): Page
    {
        return $this->paginate(
            EmailRecipient::class,
            fn (?string $cursor): EmailRecipientList => $this->single('GET', '/v1/email/broadcasts/' . rawurlencode($broadcastId) . '/recipients', EmailRecipientList::class, null, $cursor === null ? ($query ?? []) : array_merge(array_diff_key($query ?? [], ['ending_before' => true]), ['starting_after' => $cursor]), $options),
            static function (object $page): iterable {
                \assert($page instanceof EmailRecipientList);

                return $page->getData() ?? [];
            },
            static function (object $page): ?string {
                \assert($page instanceof EmailRecipientList);

                return $page->getNextCursor();
            },
        );
    }

    /**
     * Preview how many contacts a broadcast can reach before sending. The `total` value includes every contact in the audience. The `addressable` value counts contacts with an email address. The `sendable` value counts contacts that are not suppressed for the broadcast's category and can receive the broadcast. These describe the audience, not delivery, and sending does not change them: to find out what happened to a broadcast that has sent, read its recipients or its events. The broadcast must already have an audience selected.
     *
     * @example Preview how many contacts the audience holds
     * $counts = $bird->broadcasts->counts('eb_01krdgeqcxet5s7t44vh8rt9mg');
     * echo $counts->getTotal(), ' ', $counts->getAddressable(), ' ', $counts->getSendable();
     */
    public function counts(string $broadcastId, ?RequestOptions $options = null): EmailBroadcastCounts
    {
        return $this->single('GET', '/v1/email/broadcasts/' . rawurlencode($broadcastId) . '/counts', EmailBroadcastCounts::class, null, null, $options);
    }

    /**
     * List the destination URLs a broadcast's recipients clicked, each with its exact click count, distinct-recipient count, and the link's own name (when the message named it). Grouped over every click event the broadcast has, not a sample; `data` is capped at the 100 most-clicked URLs and `total` is the uncapped count of distinct URLs clicked.
     *
     * @example Read the links recipients clicked
     * $links = $bird->broadcasts->listClickedLinks('eb_01krdgeqcxet5s7t44vh8rt9mg');
     * foreach ($links->getData() ?? [] as $link) {
     *     echo $link->getUrl(), ' ', $link->getClickCount(), ' ', $link->getRecipientCount(), PHP_EOL;
     * }
     */
    public function listClickedLinks(string $broadcastId, ?RequestOptions $options = null): EmailBroadcastClickedLinkList
    {
        return $this->single('GET', '/v1/email/broadcasts/' . rawurlencode($broadcastId) . '/clicked-links', EmailBroadcastClickedLinkList::class, null, null, $options);
    }

    /**
     * Check how much of a broadcast will actually send before sending it: `recipients` is how many contacts it would reach now, `allowed` is how many of those the organization's email send allowance covers, and `limited_by` names the allowance that stops the rest. The broadcast must already have an audience selected.
     *
     * @example Check the send allowance before sending
     * $quota = $bird->broadcasts->sendQuota('eb_01krdgeqcxet5s7t44vh8rt9mg');
     * if ($quota->getAllowed() < $quota->getRecipients()) {
     *     echo $quota->getLimitedBy(), ' allowance covers only ', $quota->getAllowed();
     * }
     */
    public function sendQuota(string $broadcastId, ?RequestOptions $options = null): EmailBroadcastSendQuota
    {
        return $this->single('GET', '/v1/email/broadcasts/' . rawurlencode($broadcastId) . '/send-quota', EmailBroadcastSendQuota::class, null, null, $options);
    }
}
